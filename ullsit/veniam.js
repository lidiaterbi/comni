const wrapper = function (name) {
    console.log(`Hello, ${name}! This is the wrapper function.`);
    // More complex logic can go here
};

// Call the function with an argument
wrapper('Alice');
